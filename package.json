{
  "name": "@jbrower/improv-data",
  "version": "1.0.0",
  "main": "dist/index.js",
  "files": ["dist", "src"],
  "scripts": {
    "build": "rimraf dist && tsc -p tsconfig-base.json",
    "pub": "bun run build && npm publish --access=public"
  },
  "devDependencies": {
    "@types/node": "^20.6.0",
    "rimraf": "^5.0.1",
    "typescript": "^5.2.2"
  }
}
